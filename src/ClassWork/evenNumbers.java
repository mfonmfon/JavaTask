package ClassWork;

public class evenNumbers {
    public static int[] checkForEvenNumbers(int[] numbers){
        int[] newArrays = new int[10];
        for(int index = 0; index < newArrays.length; index++){
            boolean  isEven = numbers[index] % 2 == 0;
            if(isEven){
            }
        }
        return newArrays;
    }
}
